name: repro 5665

on:
  workflow_dispatch:
  push:
    branches: [triage-repros]

jobs:
  repro:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: |
          cd .github/triage-repros/5665
          npm install
          sudo sysctl -w fs.inotify.max_user_watches=0
          node watch.js
